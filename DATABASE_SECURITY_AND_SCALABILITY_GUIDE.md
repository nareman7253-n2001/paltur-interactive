# دليل أمان وقابلية توسع قاعدة بيانات PostgreSQL لنظام PalTur

## 1. مقدمة أمنية (Database Security Testing)
لضمان حماية بيانات المستخدمين والبلاغات في منصة **PalTur**، يجب تطبيق المعايير التالية واختبارها بانتظام:

| البند الأمني | الآلية المتبعة | طريقة الاختبار |
| :--- | :--- | :--- |
| **منع حقن SQL (SQL Injection)** | استخدام Entity Framework Core بارامترات جاهزة (Parameterized Queries) وعدم دمج النصوص المباشرة. | استخدام أدوات مثل `sqlmap` أو اختبار المدخلات في النماذج برموز SQL خبيثة. |
| **التشفير أثناء النقل (SSL/TLS)** | فرض اتصال مشفر مع قاعدة بيانات PostgreSQL (Neon/Render) عبر `SslMode=Require`. | فحص شهادات الاتصال وبروتوكول التشفير باستخدام `psql` أو أدوات الشبكة. |
| **صلاحيات الوصول (Least Privilege)** | إنشاء مستخدم قاعدة بيانات خاص بتطبيق الـ API بصلاحيات محدودة على الجداول المطلوبة فقط (SELECT, INSERT, UPDATE). | محاولة تنفيذ `DROP TABLE` بحساب التطبيق والتحقق من رفض الصلاحية. |
| **تشفير كلمات المرور** | استخدام خوارزمية `PasswordHasher` المشفرة بـ PBKDF2/Argon2 وعدم تخزين كلمات المرور نصياً. | فحص جدول `Users` والتأكد من عدم وجود كلمات مرور مكشوفة. |

---

## 2. استراتيجيات قابلية التوسع (Database Scalability)
مع نمو استخدام المنصة وزيادة عدد البلاغات والفعاليات، يمكن توسيع قاعدة البيانات عبر الخطوات التالية:

### أ. الفهارس وتحسين الاستعلامات (Indexing)
يتم إضافة فهارس على الأعمدة الأكثر بحثاً لضمان سرعة الاستجابة حتى مع ملايين السجلات:
```sql
-- فهرس للبحث السريع عن البلاغات حسب الحالة وتاريخ الإنشاد
CREATE INDEX idx_complaints_status_date ON "Complaints" ("Status", "CreatedAt");

-- فهرس للبحث الجغرافي أو حسب المستخدم
CREATE INDEX idx_complaints_user ON "Complaints" ("UserId");
```

### ب. إدارة الاتصالات (Connection Pooling)
استخدام `PgBouncer` أو إعدادات EF Core المضمنة لضبط عدد الاتصالات المتزامنة (`Max Pool Size`) لتجنب استنزاف موارد خادم قاعدة البيانات.

### ج. النسخ المتماثل (Read Replicas)
فصل عمليات القراءة (مثل استعراض الخريطة والبلاغات العامة) عن عمليات الكتابة (مثل إرسال بلاغ أو تسجيل مستخدم) باستخدام خوادم قراءة متماثلة (Read Replicas).

---

## 3. أوامر الفحص والتشخيص المقترحة
لتشخيص أداء قاعدة البيانات واختبار السرعة محلياً:
```bash
# فحص حجم الجداول وقواعد البيانات
psql -h localhost -U postgres -d paltur -c "\dt+"

# تحليل خطة تنفيذ الاستعلامات البطيئة
EXPLAIN ANALYZE SELECT * FROM "Complaints" WHERE "Status" = 'pending';
```
